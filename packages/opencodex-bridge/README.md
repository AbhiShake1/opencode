# @opencodex/bridge

OpenCodex runtime bridge package.

Exports:

- `OpenCodeBridge` interface
- `OpenCodeSdkBridge` implementation
- `createOpenCodeBridge()` factory

The bridge abstracts OpenCode SDK operations for local and remote targets.
